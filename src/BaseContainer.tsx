import styled from 'styled-components';

const BaseContainer = styled.div`
  margin: 25px 0;

  h2 {
    margin-bottom: 50px;
  }

  a {
    color: inherit;

    &:hover {
      color: #dddddd;
    }
  }
`;

export default BaseContainer;
